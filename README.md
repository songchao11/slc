### 使用
```
show linux command cheat sheet

Usage:
  slc [command]

Available Commands:
  help        Help about any command
  show        Show the specified command usage.

Flags:
  -h, --help   help for slc

Use "slc [command] --help" for more information about a command.
```
查询 cd 命令详情
```
slc show cd
```